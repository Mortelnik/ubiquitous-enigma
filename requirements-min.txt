# Минимальный набор для запуска API, очереди и саммаризации БЕЗ локального
# распознавания речи и диаризации (ENABLE_DIARIZATION=false и без обработки аудио).
# Удобно для CI, smoke-тестов и разработки фронтенда.
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
python-dotenv>=1.0
celery>=5.3
redis>=5.0
ollama>=0.3
pytest>=8.0
httpx>=0.27
