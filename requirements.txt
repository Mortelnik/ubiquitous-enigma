# ─── Backend / API ────────────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
python-dotenv>=1.0

# ─── Очередь задач ──────────────────────────────────────────────────────────────
celery>=5.3
redis>=5.0

# ─── Распознавание речи (Whisper) ────────────────────────────────────────────────
faster-whisper>=1.0
whisperx>=3.3
silero-vad>=5.1
tqdm>=4.66
librosa>=0.10

# ─── Диаризация (NeMo + кластеризация) ────────────────────────────────────────────
# Тяжёлые зависимости. Нужны только если ENABLE_DIARIZATION=true.
# Для RTX 50xx/Blackwell не опускайтесь ниже cu130.
# При pip-установке torch ставьте отдельно под вашу платформу/CUDA:
# https://pytorch.org/get-started/
torch
torchvision
torchaudio
nemo_toolkit[asr]>=1.30
soundfile>=0.12
numpy>=1.24
scikit-learn>=1.3

# ─── Генерация саммари (Ollama) ──────────────────────────────────────────────────
ollama>=0.3

# ─── Тесты ───────────────────────────────────────────────────────────────────────
pytest>=8.0
httpx>=0.27
